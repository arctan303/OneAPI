# WS-001：纯 Worker 官方 Responses WebSocket 握手诊断

状态：诊断实施、无网络验证、fresh R2聚焦复核、部署、一次真实握手及清理均完成；上游403，传输验收未通过。2026-09-07，属于Phase-02继续排障，详见[真实验证记录](../verification/WS-001.md)。临时开关已删除，账号和其他服务保持不变；纯Worker目标仍受阻。

依据：用户要求自主持续推进纯 Worker。官方 Codex 固定版本 ac192cd7937b0d73edc6dffe009940ae53782dd4 的 responses_websocket.rs 提供 probe_handshake（不发送任何请求frame）；core/src/client.rs 定义 OpenAI-Beta: responses_websockets=2026-02-06。Cloudflare Workers 支持客户端 WebSocket upgrade。因此检查被拒绝的普通 HTTP 路径以外、官方实际使用的传输方式，是有来源的有限假设，不保证成功。

来源：
- https://github.com/openai/codex/blob/ac192cd7937b0d73edc6dffe009940ae53782dd4/codex-rs/codex-api/src/endpoint/responses_websocket.rs
- https://github.com/openai/codex/blob/ac192cd7937b0d73edc6dffe009940ae53782dd4/codex-rs/core/src/client.rs
- https://developers.openai.com/api/docs/guides/websocket-mode
- https://developers.cloudflare.com/workers/runtime-apis/websockets/

范围：新增管理员临时 POST /admin/diagnostics/websocket，只接受空JSON对象且无query。Env.ONEAPI_WS_DIAGNOSTIC严格等于true字符串才启用，默认503关闭。沿用原管理员鉴权/CSRF，不给普通key权限。直接读取现有云端账号凭据，不refresh、不迁移或导出，不改普通/v1路径。

固定请求：HTTPS chatgpt.com/backend-api/codex/responses，GET upgrade:websocket，沿用本项目UA/originator/version/账号认证头及官方WebSocket beta头。Worker标准fetch与正常TLS校验，manual拒绝重定向，5秒连接限时。禁止自选URL/模型/消息、禁止所有send帧/response.create/预热。101+webSocket后accept，最多观察300ms内是否close/error/message，只记录布尔/关闭码，不记录事件正文或reason，然后finally关闭。非101保留现有安全错误诊断并停止，200不算WebSocket成功。

预算：真实云端最多1次握手，0生成、0资源目录读取、0重试、0刷新。握手通过仍不足以证明token权限、生成、额度或模型目录可用。根据证据再安排后续，不把主API切换到未经验证的传输。

风险R2：管理员新增触及云端真实凭据的网络入口和短暂连接，可能越权、外带凭据或留下连接。需fixed-target/frame-free/有界超时+关闭/元数据过滤测试，未鉴权和普通key被拒绝、默认关闭、账号状态不变，fresh reviewer通过后才部署。只操作oneapi，保留原三Secrets、原DO身份和其他服务。运行后移除临时enable开关，回读503与服务健康。未对外发支持工单。

验收：无网络fixture验证101、非101、立即关闭、错误、超时、零send与cleanup；typecheck及相关gateway测试、部署构建及敏感审计、独立审查；真实安全摘要、调用计数、前后状态及关闭回执。实现Sol/high，主会话负责官方资料、脚本、部署和收尾。