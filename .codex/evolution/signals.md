# 自进化信号

| ID | 关联规则 | 问题类别 | 摘要 | 首次出现 | 最近出现 | 次数 | 严重度 | 状态 |
| --- | --- | --- | --- | --- | --- | ---: | --- | --- |
| EVO-001 | dev-builder / reviewer：敏感文件边界 | 工具间接读取与范围失控 | 明确禁止真实凭据读取时，测试插件自动加载 .dev.vars；审查目录级 git diff 又遍历真实 .dev.vars/.wrangler | 2026-09-06 | 2026-09-06 | 2 | 隐私边界 | 待处理 |

## 证据

<!--
### EVO-001
- 用户纠正 / 报错 / 测试 / 审查 / 规则冲突：
- 处理说明：
-->

### EVO-001

- AUTH-001 实施代理：早期 Vitest/Wrangler 默认自动加载真实 `.dev.vars`，不是手工读取，仍属非预期读取；随后使用显式 test environment 与 `.dev.vars.test`。最终 Mock 23/23、SDK/typecheck/build 通过。没有输出文件值；当次有明确 Mock 证据，未观察到真实上游请求或真实 Demo 存储写入。见 `docs/verification/AUTH-001-implementation.md`。
- AUTH-001 reviewer：为快照统计使用对整个工作区的目录级 `git diff --no-index --stat`，遍历 `.dev.vars`/`.wrangler`；stat 也可能读取内容，不能把未输出值说成未读取。已停止目录级比较，后续限定显式文件白名单，未报告外部请求或文件修改。见本次审查记录。
- 当前纠正：工程测试配置隔离已落实；报告修正绝对化的“未读取”，保留真实边界。审查继续复用同一实例，只核验显式白名单的实质修复，禁止重复目录扫描。
- 规则层未改动：现有授权边界已明确禁止，尚未形成并验证能约束隐式依赖加载/目录命令范围的 Prompt 回归用例；按 EVOLUTION 协议保留待处理信号，不以新增泛化规则代替行为验证，也不阻塞本次产品交付。